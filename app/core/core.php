<?php 

 class Core
 {
    private $url;

    public function __construct()
 }
 {
    public function start($request)
    {
        var_dump($request);
        //$this-> url = explode('/', $request['url']);

        var_dump($this->url);
    }
 }
